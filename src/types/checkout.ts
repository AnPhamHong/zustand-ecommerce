
export interface FormData {
  email: string;
  cardNumber: string;
  expiry: string;
  cvc: string;
  address: string;
  city: string;
  state: string;
  postalCode: string;
  sameAsShipping: boolean;
}